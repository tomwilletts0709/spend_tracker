[project]
name = "brainlabs-task"
version = "0.1.0"
description = "brainlabs Task"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.15",
    "djangorestframework>=3.17.1",
    "gunicorn>=26.0.0",
]
